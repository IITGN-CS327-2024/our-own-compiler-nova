var int x = 1;
if (x != 0) then {
    println(5/x);
}; 
else then {
    throw DivisionByZeroError;
};

var int y = 1;
loop through (y < 5) {
    y = y + 1;
};

try {
    printNum(4);
};
catch (e) {
    println("Error");
};
