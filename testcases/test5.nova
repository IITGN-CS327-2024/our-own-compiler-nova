# Overall testcase
var int counter = 5;
var string greeting = "Hello, World!";
println(greeting[7]);
var array numbers = [5, 4, 3, 2];
println(numbers.length);
println(numbers.head);
println(numbers.tail);
numbers.cons(6);
println(numbers.head);
var int result = 10 - 6 * 3;
var bool isGTE = (result >= 0);
var bool condn = true or false and not false;
var int value = 9;
if (value > 5) {
    println(value);
};
else {
    println("Below threshold");
};
var int index = 0;
loop (index < 5) {
    index = index + 1;
    if (index == 3) {
      pass;
    };
    println("flag");
};
try {
var string result = "Hello" + 42; 
};
catch (TypeError) {
println("Invalid operation");
};
